#include <bits/stdc++.h>
using namespace std;

int main() {
	
    int T;
    cin >> T;  

    while (T--) {
        int N;
        cin >> N;  
        
        int tuesdays = (N / 7) + ((N % 7 >= 2) ? 1 : 0);
        
        cout << tuesdays << endl;
    }

    return 0;
}
